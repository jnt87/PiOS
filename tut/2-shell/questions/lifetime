it is saying that A could be the lifetime that the function is called in and that b will be the lifetime of the return value, so the return value could never outlive the function and would essentially not return anything.
